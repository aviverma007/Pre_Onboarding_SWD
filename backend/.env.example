# Copy to .env and fill in. .env is git-ignored — never commit real secrets.
PORT=5098

# SQL Server (the app auto-creates its tables on first start)
DB_SERVER=192.168.66.33
DB_PORT=1433
DB_DATABASE=PreOnboardingDB
DB_USER=smartapp_user
DB_PASSWORD=Smart$54321

# Shared login passwords (change these). One password per role for now.
LOGIN_PASSWORD=SmartWorld@2026
